{
  "name": "@web/root",
  "workspaces": [
    "./apps/*",
    "./packages/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "dev": "nx run-many -t dev",
    "lint": "nx run-many -t lint"
  },
  "type": "module",
  "devDependencies": {
    "@nx/workspace": "19.8.0",
    "nx": "19.8.0"
  }
}
